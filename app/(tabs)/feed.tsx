import { PinboardFeedScreen } from '@/components/PinboardFeedScreen'

/** Home tab: job/project-linked production updates (`job_pinboard_posts`). */
export default function FeedTab() {
  return <PinboardFeedScreen />
}
